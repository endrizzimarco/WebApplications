.row
  - unless @movies.nil?
    -@movies.each do |movie|
      .col-sm-2.col-md-3
        .thumbnail
          %a{href: "/view?id=#{movie.id}"}
            -if @index
              %img{src: "#{movie.img_path}", alt: "Poster"}
              %td= link_to 'Show', movie
              %td= link_to 'Edit', edit_movie_path(movie)
              %td= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' }
            -else 
              %img{src: "#{@image_path}w780#{movie.poster_path}", alt: "Poster"}
