.row
  -unless @movies.nil?
    -@movies.each do |movie|
      .col-sm-2.col-md-3
        .thumbnail
          %a{href: "/movies/#{movie.id}"}
            -# View for saved movies (my movies, favourites)
            -if action_name == 'index'
              %img{src: "#{movie.img_path}", alt: "Poster"}
              %td= link_to 'Show', movie
              %td= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' }
            -else # View for non-saved movies (homepage, search)
              %img{src: "#{@image_path}w300_and_h450_bestv2#{movie.poster_path}", alt: "Poster"}
          %h5 
            Average vote:           
            %span.badge.badge_default= "#{movie.vote_average}/10"